# Video

Put local reel clips here only if file sizes are reasonable.

For large videos, prefer Vimeo, YouTube, ArtStation, or SyncSketch links and paste them into `index.html` or `assets/js/data.js`.
